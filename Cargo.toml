[package]
name = "microegg"
version = "0.1.0"
edition = "2024"

[dependencies]
derive_more = { version = "2.1.1", features = ["display", "from", "into"] }
indexmap = "2.13.0"
symbol_table = { version = "0.5.0", features = ["global"] }
